"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CustomEditor = void 0;
const __1 = require("..");
const utils_1 = require("../utils");
const _1_73_0_1 = require("../../locators/1.73.0");
const constants_1 = require("../../constants");
/**
 * Page object for custom editors
 *
 * @category Editor
 */
let CustomEditor = class CustomEditor extends __1.Editor {
    constructor() {
        super(...arguments);
        /**
         * @private
         */
        this.locatorKey = 'Editor';
    }
    /**
     * Check if the editor has unsaved changes
     * @returns Promise resolving to true if there are unsaved changes, false otherwise
     */
    async isDirty() {
        const tab = await this.getTab();
        const klass = await tab.elem.getAttribute('class');
        return klass.includes('dirty');
    }
    /**
     * Save the editor
     */
    async save() {
        const tab = await this.getTab();
        await tab.elem.addValue([constants_1.CMD_KEY, 's']);
    }
    /**
     * Open the Save as prompt
     *
     * @returns InputBox serving as a simple file dialog
     */
    async saveAs() {
        const tab = await this.getTab();
        await tab.elem.addValue([constants_1.CMD_KEY, 'Shift', 's']);
        const inputBox = browser.$(this.locatorMap.InputBox.elem);
        await inputBox.waitForExist({ timeout: 5000 });
        return new __1.InputBox(this.locatorMap);
    }
};
CustomEditor = __decorate([
    (0, utils_1.PageDecorator)(_1_73_0_1.Editor)
], CustomEditor);
exports.CustomEditor = CustomEditor;
//# sourceMappingURL=CustomEditor.js.map