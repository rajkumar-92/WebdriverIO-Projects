"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CustomTreeSection = void 0;
const TreeSection_1 = require("../TreeSection");
const CustomTreeItem_1 = require("./CustomTreeItem");
const utils_1 = require("../../../utils");
const _1_73_0_1 = require("../../../../locators/1.73.0");
/**
 * Custom tree view, e.g. contributed by an extension
 *
 * @category Sidebar
 */
let CustomTreeSection = class CustomTreeSection extends TreeSection_1.TreeSection {
    constructor() {
        super(...arguments);
        /**
         * @private
         */
        this.locatorKey = ['ViewSection', 'CustomTreeSection'];
    }
    async getVisibleItems() {
        const items = [];
        const elements = await this.itemRow$$;
        for (const element of elements) {
            // eslint-disable-next-line @typescript-eslint/no-unsafe-argument
            items.push(await new CustomTreeItem_1.CustomTreeItem(this.locatorMap, element, this).wait());
        }
        return items;
    }
    async findItem(label, maxLevel = 0) {
        await this.expand();
        const container = await this.rowContainer$;
        await container.waitForExist({ timeout: 5000 });
        await container.addValue(['Home']);
        let item;
        const elements = await container.$$(this.locators.itemRow);
        for (const element of elements) {
            const temp = await element.$$(this.locators.rowWithLabel(label));
            if (temp.length > 0) {
                const level = +await temp[0].getAttribute(this.locatorMap.ViewSection.level);
                if (maxLevel < 1 || level <= maxLevel) {
                    // eslint-disable-next-line @typescript-eslint/no-unsafe-argument
                    item = await new CustomTreeItem_1.CustomTreeItem(this.locatorMap, element, this).wait();
                }
            }
        }
        return item;
    }
};
CustomTreeSection = __decorate([
    (0, utils_1.PageDecorator)({ ..._1_73_0_1.ViewSection, ..._1_73_0_1.CustomTreeSection })
], CustomTreeSection);
exports.CustomTreeSection = CustomTreeSection;
//# sourceMappingURL=CustomTreeSection.js.map